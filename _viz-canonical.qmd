```{ojs}
{
  document.getElementById("viz-loading")?.remove()
  return linePlot(filtered.filter(d => Number.isFinite(d.words)), {
    y: "words",
    title: "Reduction: Total speaker utterance length across rounds",
    yLabel: "Length (words)",
    ylim: [0, 60],
    groupingVar: grouping,
    facetVar: faceting,
    indivLines: indiv_lines,
    stageOneOnly: stage_one_only,
    containerWidth: mainWidth,
    logX: log_round_axis,
    logY: log_y_axis,
    logLogTrend: true
  })
}
```

```{ojs}
linePlot(filtered.filter(d => Number.isFinite(d.accuracy)), {
  y: "accuracy",
  title: "Accuracy across rounds",
  yLabel: "Accuracy",
  groupingVar: grouping,
  facetVar: faceting,
  indivLines: indiv_lines,
  stageOneOnly: stage_one_only,
  containerWidth: mainWidth,
  logX: log_round_axis
})
```

```{ojs}
linePlot(filtered.filter(d => Number.isFinite(d.rt)), {
  y: "rt",
  title: "Response time across rounds",
  yLabel: "Response time (s)",
  groupingVar: grouping,
  facetVar: faceting,
  indivLines: indiv_lines,
  stageOneOnly: stage_one_only,
  containerWidth: mainWidth,
  logX: log_round_axis,
  logY: log_y_axis,
  logLogTrend: true
})
```
